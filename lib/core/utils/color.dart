import 'package:flutter/material.dart';

class AppColors {
  // Primary & Background Colors
  static const Color primaryColor = Color(0xff6f28da); // Chosen primary color
  static const Color darkBackground = Color(0xff101826);
  static const Color lightBackground = Color(0xfffefeff);

  // Text Colors
  static const Color textPrimaryDark = Color(0xFFfefeff);
  static const Color textSecondary = Color(0xFF9ca3af);

  // Button Colors
  static const Color buttonPrimary = Color(0xFF6f28da);
}